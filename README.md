# export

```bash
pixi run marimo export html-wasm titanic.py -o export --mode run
```

# run dev server

```bash
❯ python -m http.server --directory export
```

# open the web page
```
http://localhost:8000
```
